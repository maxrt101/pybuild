{
  "build_dir": "{topdir}/target",
  "cache_file": "{build_dir}/cache.json",

  "cpp": {
    "compiler": "g++",
    "ar": "ar",
    "cxxflags": ["-std=c++17"],
    "includes": ["{build_dir}/include"],
    "libdirs": ["{build_dir}/lib"]
  }
}
